#include <iostream>
#include <stdexcept>
#include <string>
#include <fstream>
#include <sstream>
#include <stdlib.h>
#define BLUE = system(1), YELLOW = system(6), BLACK = system(0), RED = system(4)
#define COLORS {BLUE, YELLOW, BLACK, RED}


using namespace std;

namespace pandemic{
    class Color{
        public:
            Color();
            Color(int x);

            

    };
}
